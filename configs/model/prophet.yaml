name: "prophet"
growth: "linear"
seasonality_mode: ${dataset.prophet_seasonality_mode}
changepoint_prior_scale: 0.05
seasonality_prior_scale: 10.0
daily_seasonality: "auto"
weekly_seasonality: "auto"
yearly_seasonality: "auto"